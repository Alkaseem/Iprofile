
<% include ../partials/header %>

<section class="bg-light page-section" id="team">
        <div class="container">
          <div class="row">
            <div class="col-lg-12 text-center">
              <% include ../partials/messages %>
              <h2 class="section-heading text-uppercase">Our Amazing Dashboard</h2>
              <a class="btn btn-dark btn-md" href="/new">create your IProfie Here!</a>
              <h3 class="section-subheading text-muted mt-3">Tell Us About <em>You!</em></h3>
            </div>
          </div>
          <div class="row">
              <% profiles.forEach(function(profile){ %>
            <div class="col-sm-4">
              <div class="team-member">
                <img class="mx-auto rounded-circle" src="<%= profile.image %>" alt="">
                <h4><%= profile.fullName %></h4>
                <p class="text-muted"><%= profile.rank %></p>
                <ul class="list-inline social-buttons">
                  <!-- <% if(currentUser && profile.author.id.equals(currentUser._id)) { %>
                  <li class="list-inline-item">
                    <a href="#">
                      <i class="fas fa-trash-alt"></i>
                    </a>
                  </li>
                  <li class="list-inline-item">
                    <a href="#">
                      <i class="fas fa-pencil-alt"></i>
                    </a>
                  </li>
                <% } %> -->
                  <li class="list-inline-item">
                    <a href="/web-devs/<%= profile._id %>">
                      <i class="fa fa-user"></i>
                    </a>
                  </li>
                </ul>
              </div>
            </div>
            <% }); %>  
          </div>
          <div class="row">
            <div class="col-lg-8 mx-auto text-center">
              <p class="large text-muted">Lorem ipsum dolor sit amet, consectetur adipisicing elit. Aut eaque, laboriosam veritatis, quos non quis ad perspiciatis, totam corporis ea, alias ut unde.</p>
            </div>
          </div>
        </div>
      </section>
    

<% include ../partials/footer %>